<?php

/**
 * Database credentials for XAMPP / WAMP / LAMP / MAMP.
 *
 * Default MAMP values are used below (root / root, port 8889).
 * Change only these values if your local MySQL differs — no other code changes needed.
 *
 * Common defaults:
 *   MAMP:  host=127.0.0.1, port=8889, user=root, pass=root
 *   XAMPP: host=127.0.0.1, port=3306, user=root, pass= (empty)
 *   WAMP:  host=127.0.0.1, port=3306, user=root, pass= (empty)
 */
return [
    'host' => '127.0.0.1',
    'port' => '8889',
    'dbname' => 'purchase_entry',
    'username' => 'root',
    'password' => 'root',
    'charset' => 'utf8mb4',
];
