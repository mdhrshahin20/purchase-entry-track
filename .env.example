# Copy to .env and adjust if needed (optional — defaults work for local Docker).
# docker compose reads these automatically when present.

APP_PORT=8080
DB_PUBLISH_PORT=3307
DB_DATABASE=purchase_entry
DB_USERNAME=root
DB_PASSWORD=root
